/** Accepted file extensions for the dropzone/file input. */
export const ACCEPT = ".md,.zip";

/** Drawer width (px), matching the design system's default drawer. */
export const DRAWER_WIDTH = 560;
